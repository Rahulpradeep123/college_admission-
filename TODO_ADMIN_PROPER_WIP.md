# Admin section “proper way” - work plan (WIP)

- [ ] Refactor `Shivupro/run.py`: add `init_db()` and call it at startup
- [ ] Fix admin register SQL injection (`/regActionnn`) by using parameterized queries
- [ ] Keep/align admin routes & template actions (login/register/dashboard/logout)
- [ ] Verify admin dashboard loads and delete action works
- [ ] Smoke test after running Flask

